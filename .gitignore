# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.env
.venv/
venv/

# Build artifacts — too big for git, attached to GitHub Release instead
dist/
build/
dist_installer/
*.log
build_output.log
build_run.log
hotkey_debug*.log
app_out.log
app_err.log

# Bundled transcription model (~148 MB) — downloaded at build time via
# faster_whisper.download_model("base"), shipped inside the installer, never
# committed to git. Regenerate with: python -c "from faster_whisper import
# download_model; download_model('base', output_dir='models/base')"
models/

# Test / scratch
test_err.txt
test_log.txt
hide
see-through,+

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Note: FreeFlow.spec IS committed (required to reproduce the build)

# Local Netlify folder
.netlify

# Fichier .exe payant hébergé sur Netlify (pas dans git — >100 Mo)
docs/dl-priv-*/
